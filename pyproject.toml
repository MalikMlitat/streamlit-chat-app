[project]
name = "streamlit-chat-app"
version = "0.1.0"
requires-python = ">=3.11"
authors = [{ name = "Malik Mlitat"}]
dependencies = ["streamlit"]

[dependency-groups]
dev = ["pytest", "pre-commit"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
src = ["."]
